# BigJasBoxing — Website

Files:
- index.html
- styles.css
- script.js
- /assets/ (place images: profile.jpg, hero.jpg, img1.jpg... and sponsorship_packet.pdf)

To preview locally:
1. Place files in one folder.
2. Open `index.html` in your browser.

Deploy:
- GitHub Pages: push to a repo and enable Pages from the repository settings (branch `main` / folder `root`).
- Netlify: drag-and-drop the folder into Netlify Drop (free) or connect GitHub for auto-deploys.

Replace placeholder images and the PDF at `assets/sponsorship_packet.pdf`.
